namespace emiteat.NexUI.Designer.Editor.ScreenFlow
{
    /// <summary>Marker type used only for GraphView port typing on the Screen Flow graph (transitions
    /// are flow edges, carrying no runtime data). Mirrors <c>UIGraphFlowSignal</c> in the Motion Graph
    /// v2 editor.</summary>
    public sealed class ScreenFlowSignal
    {
    }
}
